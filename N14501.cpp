#include "iostream"
#include <ios>
using namespace std;

int n,t[16],p[16],idx,cnt=1,sum,max1=0;

int main(){
    cin.tie(0);
    ios_base::sync_with_stdio(0);
    cin >> n;
    
    for(int i = 0; i<n; i++){
        cin >> t[i] >> p[i];
    }
    cout << sum;
}

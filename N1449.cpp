#include "iostream"
#include "algorithm"
#include <algorithm>
using namespace std;

int main(){
    int n,l,gap;
    cin >> n >> l;
    int pos[1000];
    for(int i = 0; i<n; i++){
        cin >> pos[i];
    }
    sort(pos,pos+n);
    for(int i = 0; i<n; i++){
        
    }
    
}